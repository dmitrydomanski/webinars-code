extends layout

block content
        h3 #{title}
        div.row
            each article, i in articles
                div.col-md-4.mt-5
                    h4 #{article.title}
                    p(style="min-height: 96px; margin-top: 5px") #{article.body}
                    a.btn.btn-primary(href=`/articles/${article._id}`) Read more
